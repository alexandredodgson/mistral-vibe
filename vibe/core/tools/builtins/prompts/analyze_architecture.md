Analyzes the project architecture by scanning imports and generating a Mermaid dependency graph.
You can specify the 'depth' to control the granularity of the graph (e.g., 1 for top-level packages, 2 for subpackages).
If 'update_docs' is true, it will update the ARCHITECTURE.md file.
